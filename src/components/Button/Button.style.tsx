import { StyleSheet } from "react-native"

const style = StyleSheet.create({
    container: {
        alignItems: 'center',
    },

    text: {
        color: 'darkgreen'
    }
})

export default style